<div>
    <input class="input border-radius @((Type == "submit") ? "submit" : "primary")" type="button" value="@Value"/>
</div>

@code {
    [EditorRequired]
    [Parameter]
    public string Value { get; set; } = string.Empty;

    [Parameter]
    public string Type { get; set; } = string.Empty;
}